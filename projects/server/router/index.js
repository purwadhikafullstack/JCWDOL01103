const authRouter = require("./auth");
const profileRouter = require("./profile");
const productRouter = require("./product");

module.exports = {
  authRouter,
  profileRouter,
  productRouter,
};
